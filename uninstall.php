<?php
/**
 * Snap Carousel — Block Style — Uninstall
 *
 * Fired when the plugin is deleted via the WordPress admin.
 * This plugin does not store any data in the database,
 * so no cleanup is required.
 *
 * @package SnapCarousel
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
